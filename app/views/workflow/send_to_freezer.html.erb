<h4>Scan an IU Barcode to Send to Freezer</h4>
<%= form_for 'physical_object', url: process_send_to_freezer_path do |f| %>
    <table>
      <tr>
        <th>Scan IU Barcode</th>
        <td><%= f.text_field :iu_barcode %></td>
      </tr>
      <tr>
        <td><%= f.submit 'Mark Sent' %> </td>
        <td>to <b>Freezer</b></td>
      </tr>
    </table>
<% end %>
<h4 class="m_bottom_10px">Physical Objects <i>On Site</i></h4>
<%= render partial: 'physical_objects_table' %>
<%= render partial: 'physical_objects/physical_object_metadata_js' %>